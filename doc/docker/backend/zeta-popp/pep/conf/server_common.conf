http2 on;

# A_26920: ZETA/ASL mindestens HTTP-Version 1.1
if ($reject_proto) {
    return 444;
}

# Telemetrie: Trace-Kontext wird an die Backend-Dienste weitergegeben
otel_trace on;
otel_trace_context propagate;

location /doc/ {
  pep off;
}
